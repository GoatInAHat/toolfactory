# Generated by toolfactory. Do not edit; run `toolfactory build` to regenerate.

services:
  openclaw:
    image: ghcr.io/openclaw/openclaw:latest
    volumes:
      - .:/work
      - PLUGIN_DATA:/data
    env_file:
      - .env
    command:
      - sh
      - -c
      - openclaw plugins install --link /work/hosts/openclaw --force --accept-capabilities && openclaw plugins inspect toolfactory --runtime --json
  hermes:
    image: buildpack-deps:bookworm
    volumes:
      - .:/work
      - PLUGIN_DATA:/data
    environment:
      HERMES_HOME: /data
    env_file:
      - .env
    command:
      - bash
      - -c
      - curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash -s -- --commit 180291162ff4df0d42b5dc4fecd08005cf7cebf9 --skip-setup --skip-browser --skip-computer-use --non-interactive --no-skills && hermes plugins doctor /work/hosts/hermes/toolfactory_hermes --ci
volumes:
  PLUGIN_DATA: {}
